version: '3.3'
services:
  ria-golang:
    environment:
    -   APP_PORT=:8080
    -  MONGO_USERNAME=root
    -  MONGO_PASSWORD=example
    -  MONGO_URL=mongodb://root:example@ria-mongo:27017/?maxPoolSize=70